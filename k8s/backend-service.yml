apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: sarai
spec:
  selector:
    app: sarai-backend
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
  type: ClusterIP
